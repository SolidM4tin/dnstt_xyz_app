name: Build & Release Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      # 1️⃣ کلون ریپو
      - uses: actions/checkout@v4

      # 2️⃣ کلون Flutter main branch (برای Dart جدید)
      - name: Clone Flutter main branch
        run: git clone https://github.com/flutter/flutter.git -b main flutter-sdk

      # 3️⃣ نمایش نسخه Flutter و Dart
      - name: Flutter version
        run: flutter-sdk\bin\flutter.bat --version

      # 4️⃣ فعال کردن Windows desktop
      - name: Enable Windows desktop
        run: flutter-sdk\bin\flutter.bat config --enable-windows-desktop

      # 5️⃣ نصب dependencies
      - name: Get dependencies
        run: flutter-sdk\bin\flutter.bat pub get

      # 6️⃣ بیلد Windows release
      - name: Build Windows release
        run: ./scripts/build_windows.sh release

      # 7️⃣ آپلود exe به GitHub Release
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v2.2.0
          name: Windows Build v2.2.0
          body: "Windows release built automatically by GitHub Actions."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows exe to Release
        uses: softprops/action-gh-release@v1
        with:
          files: windows/**/*.exe  # wildcard برای پیدا کردن exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
